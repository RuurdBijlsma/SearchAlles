class SearchResult {
    constructor({id, iconUrl, title, getContent}) {
        this.id = id;
        this.iconUrl = iconUrl;
        this.title = title;
        this.getContent = getContent;
    }
}